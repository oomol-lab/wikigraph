Help Type: command
Command: wikigraph <chapter-uri>/summary set

Purpose:
  Fill a Reading Graph-stage chapter with a manual final summary.

Usage:
  wikigraph <chapter-uri>/summary set [text] [--input <path>] [--llm <json>] [--help|-h]

Input:
  - Summary text is read from a positional value, `--input <path>`, or stdin.
  - Summary text is stored as the chapter's final summary.
  - A common workflow is `read > /tmp/summary.md`, edit the file, then pass it back with `--input`.

Preconditions:
  - The chapter must be `reading-graph`.
  - If the chapter is `source`, run:
    wikigraph wikg://local/job add --input <archive-uri|chapter-uri> --task reading-graph --accept-cost

Behavior:
  - Moves the chapter to `reading-summary`.
  - Does not generate or modify Reading Graph data.
  - Does not call an LLM provider.

See also:
  - wikigraph wikg://local/job --help
  - wikigraph wikg://book.wikg/chapter/3/state --help
  - wikigraph help readiness
  - wikigraph help runtime
  - wikigraph --help
