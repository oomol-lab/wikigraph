Help Type: topic
Topic: env

Environment Variable Reference

Scope:
  These variables let shell invocations override machine defaults without editing `~/.wikigraph/config.json`.
  `--llm` overrides LLM environment variables for one run.
  Environment variables override the config file.
  `--prompt` still overrides both for one run.

Path resolution:
  - `WIKIGRAPH_CONFIG`, `WIKIGRAPH_CACHE_DIR`, and `WIKIGRAPH_DEBUG_LOG_DIR` accept absolute paths.
  - `~` is expanded to the current user's home directory.
  - Relative paths are resolved from the current working directory.

LLM-backed operations:
  The following settings matter when digesting source input, or when queue jobs need reading-graph, reading-summary, or knowledge-graph generation:
  - `WIKIGRAPH_PROMPT`
  - `WIKIGRAPH_LLM_PROVIDER`
  - `WIKIGRAPH_LLM_MODEL`
  - `WIKIGRAPH_LLM_BASE_URL`
  - `WIKIGRAPH_LLM_NAME`
  - `WIKIGRAPH_LLM_API_KEY`
  - `WIKIGRAPH_CACHE_DIR`
  - `WIKIGRAPH_QUEUE_CONCURRENT`
  - `WIKIGRAPH_REQUEST_CONCURRENT`
  - `WIKIGRAPH_REQUEST_TIMEOUT`
  - `WIKIGRAPH_REQUEST_RETRY_TIMES`
  - `WIKIGRAPH_REQUEST_RETRY_INTERVAL_SECONDS`
  - `WIKIGRAPH_REQUEST_TEMPERATURE`
  - `WIKIGRAPH_REQUEST_TOP_P`
  - `WIKIGRAPH_REQUEST_STREAM`

Variables:
  `WIKIGRAPH_CONFIG`
    Override the config file path.
    Type: path string.

  `WIKIGRAPH_PROMPT`
    Override the default extraction prompt for reading-graph generation.
    Type: non-empty string.

  `WIKIGRAPH_LLM_PROVIDER`
    Choose the LLM provider.
    Type: one of `anthropic`, `google`, `openai`, `openai-compatible`.

  `WIKIGRAPH_LLM_MODEL`
    Choose the provider-specific model id.
    Type: non-empty string.

  `WIKIGRAPH_LLM_BASE_URL`
    Provide a custom base URL for `openai-compatible`.
    Type: non-empty string (typically a URL) such as `https://api.example/v1`.
    Notes:
      `openai` rejects this variable.
      `openai-compatible` requires it.

  `WIKIGRAPH_LLM_NAME`
    Override the SDK client name where supported.
    Type: non-empty string.

  `WIKIGRAPH_LLM_API_KEY`
    Provide provider credentials.
    Type: non-empty string.

  `WIKIGRAPH_CACHE_DIR`
    Store cached LLM responses for LLM-backed operations.
    Type: path string.

  `WIKIGRAPH_DEBUG_LOG_DIR`
    Store debug logs when debug logging is enabled.
    Type: path string.
    Notes:
      This can still matter for `.wikg` re-export through the main convert command because the app may emit debug logs there.

  `WIKIGRAPH_QUEUE_CONCURRENT`
    Control simultaneous queue jobs.
    Type: positive integer.
    Notes:
      This is separate from `WIKIGRAPH_REQUEST_CONCURRENT`, which controls concurrent LLM requests inside jobs.

  `WIKIGRAPH_REQUEST_CONCURRENT`
    Control concurrent LLM requests.
    Type: positive integer.

  `WIKIGRAPH_REQUEST_TIMEOUT`
    Control request timeout.
    Type: positive number.

  `WIKIGRAPH_REQUEST_RETRY_TIMES`
    Control retry count.
    Type: non-negative integer.

  `WIKIGRAPH_REQUEST_RETRY_INTERVAL_SECONDS`
    Control retry backoff interval in seconds.
    Type: positive number.

  `WIKIGRAPH_REQUEST_TEMPERATURE`
    Override sampling temperature.
    Type: positive number or JSON number array such as `0.2` or `[0.2, 0.4]`.

  `WIKIGRAPH_REQUEST_TOP_P`
    Override top-p sampling.
    Type: positive number or JSON number array such as `0.9` or `[0.85, 0.9]`.

  `WIKIGRAPH_REQUEST_STREAM`
    Control request streaming behavior.
    Type: `true` / `false` or `1` / `0`.

Common failures:
  - Invalid provider values are rejected immediately.
  - Invalid booleans must be `true/false` or `1/0`.
  - Invalid numeric settings must satisfy the documented integer or positive-number rules.

Where to go next:
  - Need the overview and precedence model:
    wikigraph help config
  - Need the JSON file schema instead of env overrides:
    wikigraph help config-file
  - Need provider and runtime context:
    wikigraph help runtime
  - Need failure diagnosis:
    wikigraph help troubleshoot
