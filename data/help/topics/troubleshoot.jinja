Help Type: topic
Topic: troubleshoot

Troubleshooting

Missing LLM configuration:
  Symptom:
    Source digest runs fail before processing starts.
  Check:
    `--llm`, `llm.provider` and `llm.model` in config, or the matching env vars.

Unexpected format errors:
  Symptom:
    The CLI rejects an input or output format.
  Check:
    The file extension, or pass `--input-format` / `--output-format` explicitly.

stdin or stdout misuse:
  Symptom:
    The CLI rejects interactive stdin or `--verbose` with stdout output.
  Check:
    Pipe text into stdin, or write digest output to a file when using `--verbose`.

sdpub command failure:
  Symptom:
    `sdpub` subcommands reject flags or input.
  Check:
    Inspection commands such as `info`, `toc`, `list`, `cat`, `cover`, and `meta` require `--input <path>`.
    `stage` and `chapter` actions require the `.sdpub` path as a positional argument.
    `cat` requires `--serial <id>`.
    Chapter-specific actions usually require `--chapter <id>`.

Binary cover output blocked:
  Symptom:
    `sdpub cover` refuses to write to the terminal.
  Check:
    Redirect stdout or pipe the bytes elsewhere.

Where to go next:
  - Need config-layer overview:
    spinedigest help config
  - Need environment variable details:
    spinedigest help env
  - Need config file field definitions:
    spinedigest help config-file
  - Need stream, stderr, progress, or digest-dir behavior:
    spinedigest help runtime
  - Need legal format combinations:
    spinedigest help format
  - Need to verify the command shape or unsupported flags:
    spinedigest help command
  - Need task-level alternatives:
    spinedigest help task
  - Need archive command-specific guidance:
    spinedigest help sdpub
