Help Type: topic
Topic: command

Command Overview

Object commands:
  spinedigest search <archive.sdpub> <query>
  spinedigest get <archive.sdpub> <uri>
  spinedigest related <archive.sdpub> <uri>
  spinedigest evidence <archive.sdpub> <uri>
  spinedigest pack <archive.sdpub> <uri>

Archive commands:
  spinedigest create <archive.sdpub>
  spinedigest export <archive.sdpub>
  estimate    Estimate generation cost.
  index       Show archive-level counts.
  spinedigest transform [options]

Generation:
  queue add <archive.sdpub> --chapter <id> --task <reading-graph|summary|knowledge-graph> --accept-cost

Maintenance:
  meta, cover, chapter, config status
